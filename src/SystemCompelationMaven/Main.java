package SystemCompelationMaven;

import Lambda.Employee;
import org.json.simple.JSONArray;
import org.json.simple.JSONObject;
import org.json.simple.parser.JSONParser;

import javax.xml.crypto.Data;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.text.DecimalFormat;
import java.text.SimpleDateFormat;
import java.util.List;


public class Main {
   //        private static String staffFile = "staff.txt";
  //  public static void main(String[] args) {
       // try {

/**
 *   try {
 *             String data = getDataFromFile("staff.json");
 *             JSONParser parser = new JSONParser();
 *             JSONArray array = (JSONArray) parser.parse(data);
 *             for (Object item : array){
 *                 JSONObject jsonObject = (JSONObject) item;
 *                 System.out.println(jsonObject.get("name"));
 *             }
 *                 }
 *         catch (Exception ex){
 *             ex.printStackTrace();
 *         } }
 */
/**
 private static String getDataFromFile (String path) { //превращает файл в строчку
 StringBuilder builder = new StringBuilder(); // будем добавлять строки нашего файла

 try {
 List<String> lines = Files.readAllLines(Paths.get()); // readAllLines возвращал коллекцию со строками
 for (String line : lines) {
 builder.append(line);// добавление строк из файла
 }
 } catch (Exception ex) {
 ex.printStackTrace();
 }
 return builder.toString();
 }


 */
}
